#include "Logic/cltConfiguration.h"

void cltConfiguration::GetHelpState(int* outState) {
    // Stub: real implementation reads help state from config file/registry.
    if (outState) *outState = 0;
}
